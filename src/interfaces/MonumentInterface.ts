export interface MonumentInterface {
    id: string;
    image: string;
    name: string;
    location: string;
    author: string;
    date: string;
    description: string;
    coordinates: [number, number]
}

